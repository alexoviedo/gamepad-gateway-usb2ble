{
  "name": "gamepad-gateway-usb2ble",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck:web": "tsc -p tsconfig.web.json --pretty false",
    "lint:web": "eslint webapp/**/*.js",
    "check:web": "npm run typecheck:web && npm run lint:web",
    "build:web": "rm -rf webapp/dist && mkdir -p webapp/dist && rsync -a --delete --exclude '.DS_Store' webapp/ webapp/dist/ && touch webapp/dist/.nojekyll",
    "build": "npm run build:web"
  },
  "devDependencies": {
    "@eslint/js": "^9.22.0",
    "eslint": "^9.22.0",
    "globals": "^15.15.0",
    "typescript": "^5.8.2"
  }
}
